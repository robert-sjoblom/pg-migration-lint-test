<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd">

    <changeSet id="orders-001-create-table" author="test">
        <sql>
            CREATE TABLE IF NOT EXISTS orders (
                id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
                user_id BIGINT NOT NULL REFERENCES users (id),
                status TEXT NOT NULL DEFAULT 'pending',
                created_at TIMESTAMPTZ NOT NULL DEFAULT now()
            );

            CREATE INDEX IF NOT EXISTS idx_orders_user_id ON orders (user_id);
        </sql>
    </changeSet>

    <changeSet id="orders-002-add-total-cents" author="test">
        <sql>
            ALTER TABLE orders ADD COLUMN IF NOT EXISTS total_cents BIGINT NOT NULL DEFAULT 0;
        </sql>
    </changeSet>

    <changeSet id="orders-003-add-status-index" author="test">
        <sql>
            CREATE INDEX IF NOT EXISTS idx_orders_status ON orders (status);
        </sql>
    </changeSet>

    <changeSet id="orders-004-create-order-items" author="test">
        <sql>
            CREATE TABLE IF NOT EXISTS order_items (
                id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
                order_id BIGINT NOT NULL REFERENCES orders (id),
                product_name TEXT NOT NULL,
                quantity INTEGER NOT NULL DEFAULT 1,
                unit_price_cents BIGINT NOT NULL
            );

            CREATE INDEX IF NOT EXISTS idx_order_items_order_id ON order_items (order_id);
        </sql>
    </changeSet>

</databaseChangeLog>
